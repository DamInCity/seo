# SPA fallback is now handled by:
# not_found_handling = "single-page-application" in wrangler.toml
#
# The previous catch-all rule was removed because it triggered
# Cloudflare's infinite loop detection (due to .html / index stripping logic).
